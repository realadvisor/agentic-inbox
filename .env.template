# Run pnpm setup:local to generate local-only credentials and .env.
DATABASE_URL=postgres://inbox:REPLACE_ME@127.0.0.1:55439/agentic_inbox_prototype
POSTGRES_PASSWORD=REPLACE_ME
PORT=4311

# Optional: enables Anthropic, OpenAI and other models via Vercel AI Gateway.
# In production store this using `wrangler secret put AI_GATEWAY_API_KEY`.
AI_GATEWAY_API_KEY=
